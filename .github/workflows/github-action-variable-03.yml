name: Test Vars context test
run-name: RUN Vars context test

on:
  workflow_dispatch

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      - name: "Say Hello JOB_NAME"
        run: echo "JOB_NAME : ${{ vars.JOB_NAME }}"


          
